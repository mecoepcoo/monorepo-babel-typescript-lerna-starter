{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["main", "master"],
      "message": "chore(release): publish",
      "noCommitHooks": true,
      "noPush": true,
      "noGitTagVersion": true
    }
  }
}
